apiVersion: kubedb.com/v1alpha1
kind: Etcd
metadata:
  name: quick-etcd
  namespace: demo
spec:
  replicas: 2
  version: "3.2.13"
  storage:
    storageClassName: "standard"
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi